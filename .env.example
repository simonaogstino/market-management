DATABASE_URL="file:./dev.db"
NEXTAUTH_URL="http://localhost:3000"
# Required in production (GoDaddy, etc.) — use a long random string, e.g. openssl rand -base64 32
NEXTAUTH_SECRET="dev-secret-change-in-production"
# Optional: absolute path for log files (default: <repo>/logs)
# LOG_DIR="C:/Users/simon/Projects/market-management/logs"
